# dly

个人年度书籍记录网页应用（纯前端，无需后端）。

## 功能

- 添加书名、作者、评分。
- 支持按评分排序、单条删除、清空记录。
- 自动保存到浏览器 `localStorage`，刷新不丢失。
- 一键生成年度图卡并下载 PNG。
- 支持导出 JSON 备份。

## 使用方式

1. 直接在浏览器打开 `index.html`。
2. 输入信息后点击「添加记录」。
3. 点击「生成年度图卡」导出图片，或点击「导出 JSON 备份」。
